import 'package:flutter/material.dart';

class followUsersPosts extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('follow users post page'),
      ),
    );
  }
}